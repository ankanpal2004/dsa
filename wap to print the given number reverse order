#include<stdio.h>
int main(){
    int i,arr[5];
    printf("Enter the array element:");
    for(i=0;i<5;i++){
        scanf("%d",&arr[i]);
    }
    printf("\n array in reverse order:");
    for(i=4;i>=0;i--){
        printf("%d",arr[i]);
    }
    return 0;
}
